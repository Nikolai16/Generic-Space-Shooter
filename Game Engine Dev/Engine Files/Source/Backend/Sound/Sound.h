/*
	sound class will play sounds from the game.  Actor components will be 
	able to call functions of this class which then play sounds.  This class
	plays sounds via the window so it needs that to work.
*/

//nothing will inherit from sound

class Sound{

	//init will make sure the window is ready to play sounds as well as init all its members
	//shutdown will remove its members and stop all sounds from playing as well as return an error if sounds try to play from here on out

};
